"""Poisson solvers (pluggable, per AGENT.md D5).

Placeholder for Stage 3+: ``base.PoissonSolver`` interface, ``multigrid`` (open-BC,
portable default) and ``fft_oracle`` (zero-padded isolated FFT, NVIDIA validation).
"""
